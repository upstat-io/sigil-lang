// Note: Test attribute output (#skip, #compile_fail, #fail) is not preserved
// This is because the formatter doesn't have access to the original attribute text
// and the parser stores attributes as structured data (skip_reason, expected_errors, etc.)
// TODO: Implement attribute output in formatter

@test_future () -> void = 1

@test_type_error () -> void = 42

@test_panic () -> void = 1
