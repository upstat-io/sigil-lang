// Immutable bindings with $ prefix
// Spec: 06-expressions.md § Let Bindings

@with_immutable () -> int = {
    let $x = 42;
    $x
}

@with_both () -> int = {
    let $a = 10;
    let b = 20;

    $a + b
}
