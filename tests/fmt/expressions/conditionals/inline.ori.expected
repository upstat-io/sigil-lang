// Short conditionals that fit on one line
// Spec: 16-formatting.md § Conditionals

@abs (x: int) -> int = if x > 0 then x else -x

@max (a: int, b: int) -> int = if a > b then a else b

@sign (x: int) -> int = if x > 0 then 1 else -1
