type Config = { max_timeout: int, max_retries: int, base_url: str, debug_mode: bool }

@default_config () -> Config = Config {
    max_timeout: 30,
    max_retries: 3,
    base_url: "https://api.example.com",
    debug_mode: false,
}
