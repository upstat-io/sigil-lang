// Inclusive range formatting
// Design: formatter/design/02-constructs/collections.md § Ranges

@simple () -> int = {
    let sum = 0;
    for i in 0..=10 do let sum = sum + i;
    sum
}

@count () -> int = {
    let count = 0;
    for i in 1..=100 do let count = count + 1;
    count
}
