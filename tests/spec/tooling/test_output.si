// Spec: spec/13-testing.md § Test Execution
// Design: design/11-testing/index.md § Overview
//
// Tests for test output formatting.
//
// Default output shows:
//   Test Summary:
//     N passed, N failed, N skipped (N total)
//     Completed in Xms
//
// Verbose mode (--verbose or -v) shows:
//   path/to/file.si
//     PASS: test_name (Xµs)
//     FAIL: test_name (Xµs)
//     SKIP: test_name (reason)
//
// Run this file with:
//   sigil test tests/spec/tooling/test_output.si           # default output
//   sigil test tests/spec/tooling/test_output.si --verbose # verbose output

// =============================================================================
// Helper functions
// =============================================================================

@fast_op (x: int) -> int = x

@slow_op (x: int) -> int = x + x + x + x + x

// =============================================================================
// Tests that produce output
// =============================================================================
@test_fast tests @fast_op () -> void = run(
    assert_eq(fast_op(1), 1),
    assert_eq(fast_op(100), 100),
)

@test_slow tests @slow_op () -> void = run(
    assert_eq(slow_op(1), 5),
    assert_eq(slow_op(10), 50),
)
