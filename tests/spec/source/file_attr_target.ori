#!target(os: "linux")
// Spec: 08-declarations.md § File-Level Attributes
// Tests that #!target(...) parses correctly as a file-level attribute.
//
// Note: Conditional compilation is not yet implemented (Section 13).
// This file tests PARSING only — the attribute is parsed and stored but
// does not affect compilation behavior yet.

use std.testing { assert_eq }

@greet () -> str = "hello from linux"

@test_target_attr tests @greet () -> void = run(
    assert_eq(actual: greet(), expected: "hello from linux"),
)
