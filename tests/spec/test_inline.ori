use std.testing { assert_eq }

@bar () -> void = ();

@test_bar tests @bar () -> void = {
    let result: Result<int, str> = Ok(42);
    let value = result ?? panic(msg: "oops");
    assert_eq(actual: value, expected: 42)
}
