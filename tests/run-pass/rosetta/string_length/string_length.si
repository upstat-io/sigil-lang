// Rosetta Code: String length
// Task: Find the character length of a string

// Get character length of a string
@string_length (s: str) -> int = len(.collection: s)

// Check if string is empty
@is_empty_str (s: str) -> bool = len(.collection: s) == 0

// Check if string has content
@has_content (s: str) -> bool = len(.collection: s) > 0
