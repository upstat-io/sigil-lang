// Rosetta Code: String length
// Task: Find the character length of a string

// Get byte length of a string
// Note: Uses method syntax since Len trait is pending approval
@string_length (s: str) -> int = s.len();

// Check if string is empty
@is_empty_str (s: str) -> bool = s.len() == 0;

// Check if string has content
@has_content (s: str) -> bool = s.len() > 0;
