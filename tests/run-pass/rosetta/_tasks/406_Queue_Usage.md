# Queue/Usage

**Problem:** frame|right|Illustration of FIFO behavior Create a queue data structure and demonstrate its operations. (For implementations of queues, see the FIFO task.) Operations:

**Requirements:**
- push (aka enqueue) - add element
- pop (aka dequeue) - pop first element

**Success Criteria:**
- empty - return truth value when empty
