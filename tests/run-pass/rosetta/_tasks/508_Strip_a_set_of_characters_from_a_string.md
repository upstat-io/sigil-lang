# Strip a set of characters from a string

**Problem:** Create a function that strips a set of characters from a string. The function should take two arguments: The returned string should contain the first string, stripped of any characters in the second argument: print stripchars("She was a soul stripper. She took my heart!","aei") Sh ws soul strppr. Sh took my hrt!

**Requirements:**
- a string to be stripped
- a string containing the set of characters to be stripped

**Success Criteria:**
- Program produces correct output for test cases
- Implementation matches Rosetta Code specification
