# Sum of squares

**Problem:** Write a program to find the sum of squares of a numeric vector. The program should work on a zero-length vector (with an answer of 0).

**Requirements:**
- The program should work on a zero-length vector (with an answer of 0).

**Success Criteria:**
- Program produces correct output for test cases
- Implementation matches Rosetta Code specification
