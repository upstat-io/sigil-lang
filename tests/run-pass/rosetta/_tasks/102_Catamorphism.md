# Catamorphism

**Problem:** Reduce is a function or method that is used to take the values in an array or a list and apply a function to successive members of the list to produce (or reduce them to), a single value. Show how reduce (or foldl or foldr etc), work (or would be implemented) in your language.

**Requirements:**
- Show how reduce (or foldl or foldr etc), work (or would be implemented) in your language.

**Success Criteria:**
- Program produces correct output for test cases
- Implementation matches Rosetta Code specification
