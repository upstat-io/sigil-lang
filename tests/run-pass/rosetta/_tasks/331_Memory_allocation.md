# Memory allocation

**Problem:** Show how to explicitly allocate and deallocate blocks of memory in your language. Show access to different types of memory (i.e., heap, stack, shared, foreign) if applicable.

**Requirements:**
- Show access to different types of memory (i.e., heap, stack, shared, foreign) if applicable.

**Success Criteria:**
- Program produces correct output for test cases
- Implementation matches Rosetta Code specification
