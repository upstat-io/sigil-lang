# Queue/Definition

**Problem:** frame|right|Illustration of FIFO behavior Implement a FIFO queue. Elements are added at one side and popped from the other in the order of insertion. Operations:

**Requirements:**
- push (aka enqueue) - add element
- pop (aka dequeue) - pop first element
- handle the error of trying to pop from an empty queue (behavior depends on the language and platform)
- Queue/Usage for the built-in FIFO or queue of your language or standard library.

**Success Criteria:**
- empty - return truth value when empty
