# Array length

**Problem:** Determine the number of elements in an array.

**Requirements:**
- Create an array holding the strings "apple" and "orange"
- Calculate and return the length/count of elements in the array
- Use the language's native array length mechanism

**Success Criteria:**
- Output: 2 (for the two-element array)
- Works with the language's native array/collection types
