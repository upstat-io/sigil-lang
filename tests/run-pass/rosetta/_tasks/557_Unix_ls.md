# Unix/ls

**Problem:** Write a program that will list everything in the current folder, similar to: The output must be sorted, but printing extended details and producing multi-column output is not required. Example output For the list of paths: When the program is executed in `/foo`, it should print: and when the program is executed in `/foo/bar`, it should print:

**Requirements:**
- the Unix utility “ls” or
- the Windows terminal command “DIR”

**Success Criteria:**
- Program produces correct output for test cases
- Implementation matches Rosetta Code specification
