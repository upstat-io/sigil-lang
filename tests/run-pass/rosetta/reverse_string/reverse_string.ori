// Rosetta Code: Reverse a string
// Task: Reverse a string
// BLOCKED: Requires string slice method not yet implemented

// Reverse a string recursively
// Note: Currently blocked - requires string slice operation
@reverse (s: str) -> str = s;

// Check if a string is a palindrome
// Note: Currently blocked - requires working reverse
@is_palindrome (s: str) -> bool = true;
