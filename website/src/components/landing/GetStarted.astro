---
---

<section class="get-started">
  <div class="gs-inner">
    <h2 class="gs-heading">Get Started</h2>
    <div class="gs-steps">
      <div class="gs-step">
        <h3 class="gs-step-title">1. Install</h3>
        <pre class="gs-code"><code>curl -fsSL https://ori-lang.com/install.sh | sh</code></pre>
      </div>
      <div class="gs-step">
        <h3 class="gs-step-title">2. Write your first program</h3>
        <pre class="gs-code"><code>// hello.ori
@main () -> void = print(msg: "Hello, World!")</code></pre>
      </div>
      <div class="gs-step">
        <h3 class="gs-step-title">3. Run it</h3>
        <pre class="gs-code"><code>ori run hello.ori</code></pre>
      </div>
    </div>
    <div class="gs-cta">
      <a href="/guide/01-getting-started" class="gs-link">Read the full documentation &rarr;</a>
    </div>
  </div>
</section>

<style>
  .get-started {
    padding: var(--space-16) 0;
    background: var(--color-bg-secondary);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
  }

  .gs-inner {
    max-width: var(--max-width-narrow);
    margin: 0 auto;
    padding: 0 var(--space-6);
  }

  .gs-heading {
    text-align: center;
    margin-bottom: var(--space-10);
    font-size: var(--text-3xl);
  }

  .gs-steps {
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
  }

  .gs-step-title {
    font-size: var(--text-lg);
    margin-bottom: var(--space-3);
    color: var(--color-text-primary);
  }

  .gs-code {
    background: var(--color-bg-code);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-4);
    overflow-x: auto;
    font-size: var(--text-sm);
    line-height: var(--leading-relaxed);
  }

  .gs-cta {
    text-align: center;
    margin-top: var(--space-10);
  }

  .gs-link {
    font-size: var(--text-lg);
    font-weight: var(--weight-semibold);
    color: var(--color-accent);
    text-decoration: none;
  }

  .gs-link:hover {
    color: var(--color-accent-hover);
  }
</style>
