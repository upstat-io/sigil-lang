//! Sigil IR - Re-exports from `sigil_ir`
//!
//! This module re-exports all types from the `sigil_ir` crate.
//! The `sigil_ir` crate is the single source of truth for IR types.

// Re-export everything from sigil_ir
pub use sigil_ir::*;
